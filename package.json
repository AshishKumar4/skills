{
  "name": "opencode-agent-rules",
  "version": "0.1.0",
  "private": true,
  "description": "Global OpenCode AGENTS.md and reusable skills for production-quality coding agents.",
  "type": "module",
  "bin": {
    "opencode-agent-rules": "./bin/install.mjs"
  },
  "scripts": {
    "install:local": "node ./bin/install.mjs",
    "verify": "node ./bin/install.mjs --verify"
  }
}
